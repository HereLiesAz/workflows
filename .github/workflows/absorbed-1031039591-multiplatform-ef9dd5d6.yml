# Generated by HereLiesAz/workflows. Do not edit by hand.
# source: HereLiesAz/haive:.github/workflows/multiplatform.yml
name: Build and Deploy [central]
on:
  workflow_dispatch:
    inputs:
      target_repository:
        required: true
        type: string
      target_repository_id:
        required: true
        type: string
      target_repository_owner:
        required: true
        type: string
      target_repository_owner_id:
        required: true
        type: string
      target_sha:
        required: true
        type: string
      target_check_sha:
        required: true
        type: string
      target_ref:
        required: true
        type: string
      target_ref_name:
        required: false
        type: string
      target_ref_type:
        required: false
        type: string
      target_head_ref:
        required: false
        type: string
      target_base_ref:
        required: false
        type: string
      target_actor:
        required: true
        type: string
      target_actor_id:
        required: false
        type: string
      target_event_name:
        required: true
        type: string
      target_event_json:
        required: true
        type: string
      target_inputs_json:
        required: true
        type: string
      target_vars_json:
        required: true
        type: string
      target_run_id:
        required: true
        type: string
      target_run_number:
        required: false
        type: string
      target_run_attempt:
        required: false
        type: string
      target_workflow_ref:
        required: false
        type: string
      target_workflow_name:
        required: true
        type: string
      source_workflow_path:
        required: true
        type: string
      source_sha256:
        required: true
        type: string
permissions:
  contents: read
env:
  TARGET_REPOSITORY: ${{ inputs.target_repository }}
  TARGET_REPOSITORY_ID: ${{ inputs.target_repository_id }}
  TARGET_REPOSITORY_OWNER: ${{ inputs.target_repository_owner }}
  TARGET_REPOSITORY_OWNER_ID: ${{ inputs.target_repository_owner_id }}
  TARGET_SHA: ${{ inputs.target_sha }}
  TARGET_CHECK_SHA: ${{ inputs.target_check_sha }}
  TARGET_REF: ${{ inputs.target_ref }}
  TARGET_REF_NAME: ${{ inputs.target_ref_name }}
  TARGET_REF_TYPE: ${{ inputs.target_ref_type }}
  TARGET_HEAD_REF: ${{ inputs.target_head_ref }}
  TARGET_BASE_REF: ${{ inputs.target_base_ref }}
  TARGET_ACTOR: ${{ inputs.target_actor }}
  TARGET_ACTOR_ID: ${{ inputs.target_actor_id }}
  TARGET_EVENT_NAME: ${{ inputs.target_event_name }}
  TARGET_RUN_ID: ${{ inputs.target_run_id }}
  TARGET_RUN_NUMBER: ${{ inputs.target_run_number }}
  TARGET_RUN_ATTEMPT: ${{ inputs.target_run_attempt }}
  TARGET_WORKFLOW_REF: ${{ inputs.target_workflow_ref }}
  TARGET_WORKFLOW_NAME: ${{ inputs.target_workflow_name }}
concurrency:
  group: haive-${{ inputs.target_workflow_name }}-${{ inputs.target_ref }}
  cancel-in-progress: true
jobs:
  __central_check_start:
    runs-on: ubuntu-latest
    outputs:
      check_id: ${{ steps.create.outputs.check_id }}
    steps:
      - name: Validate immutable target identity
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        shell: bash
        run: "set -euo pipefail\nrepo_json=\"$(gh api \"repos/${TARGET_REPOSITORY}\")\"\nactual_repo_id=\"$(jq -r '.id' <<<\"$repo_json\")\"\nactual_owner_id=\"$(jq -r '.owner.id' <<<\"$repo_json\")\"\nactual_owner=\"$(jq -r '.owner.login' <<<\"$repo_json\")\"\n[[ \"$actual_repo_id\" == \"$TARGET_REPOSITORY_ID\" ]] || { echo \"Repository ID mismatch\" >&2; exit 1; }\n[[ \"$actual_owner_id\" == \"103241502\" ]] || { echo \"Repository is not owned by HereLiesAz\" >&2; exit 1; }\n[[ \"${actual_owner,,}\" == \"hereliesaz\" ]] || { echo \"Repository owner mismatch\" >&2; exit 1; }\n[[ \"$TARGET_REPOSITORY_OWNER_ID\" == \"103241502\" ]] || { echo \"OIDC owner ID mismatch\" >&2; exit 1; }"
      - name: Create target check
        id: create
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          CHECK_NAME: Build and Deploy
          DETAILS_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        shell: bash
        run: "set -euo pipefail\npayload=\"$(jq -n --arg name \"$CHECK_NAME\" --arg sha \"$TARGET_CHECK_SHA\" --arg details \"$DETAILS_URL\" '{name:$name,head_sha:$sha,status:\"in_progress\",details_url:$details,output:{title:$name,summary:\"Running centrally in HereLiesAz/workflows.\"}}')\"\nresponse=\"$(gh api --method POST \"repos/${TARGET_REPOSITORY}/check-runs\" --input - <<<\"$payload\")\"\necho \"check_id=$(jq -r '.id' <<<\"$response\")\" >> \"$GITHUB_OUTPUT\""
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          repository: ${{ inputs.target_repository }}
          ref: ${{ inputs.target_sha }}
          token: ${{ github.token }}
          persist-credentials: false
      - name: Check out pinned h2g2
        uses: actions/checkout@v7
        with:
          repository: HereLiesAz/conveyance-h2g2
          ref: 8da4f0b3a764fe6ecd96f9e32c6ba7d3b2eb900a
          path: vendor/conveyance-h2g2
          token: ${{ github.token }}
          persist-credentials: false
      - name: Check out pinned Conveyance
        uses: actions/checkout@v7
        with:
          repository: HereLiesAz/Conveyance
          ref: b3e13674df9dfbcc0b35f800b57d78a305d07b03
          path: vendor/conveyance-h2g2/vendor/Conveyance
          token: ${{ github.token }}
          persist-credentials: false
      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'
      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v6
      - name: Reconstruct and verify Android signing keystore
        id: signing
        if: fromJSON(inputs.target_event_json)_name != 'pull_request'
        uses: ./.github/actions/android-keystore
        with:
          keystore-private: ${{ secrets.KEYSTORE_PRIVATE }}
          keystore-rsa: ${{ secrets.KEYSTORE_RSA }}
          keystore-public: ${{ secrets.KEYSTORE_PUBLIC }}
          keystore-chain: ${{ secrets.KEYSTORE_CHAIN }}
          keystore-password: ${{ secrets.KEYSTORE_PASSWORD }}
          key-alias: ${{ secrets.KEY_ALIAS }}
          key-password: ${{ secrets.KEY_PASSWORD }}
          expected-sha256: ${{ secrets.KEYSTORE_SHA256 }}
          expected-sha1: ${{ secrets.KEYSTORE_SHA1 }}
          expected-owner: ${{ secrets.KEYSTORE_OWNER }}
      - name: Export Android signing path
        if: fromJSON(inputs.target_event_json)_name != 'pull_request'
        run: "echo \"HAIVE_KEYSTORE_PATH=${{ steps.signing.outputs.keystore-path }}\" >> \"$GITHUB_ENV\"\necho \"HAIVE_KEYSTORE_TYPE=${{ steps.signing.outputs.store-type }}\" >> \"$GITHUB_ENV\"\n"
      - name: Select Android release versionCode
        if: fromJSON(inputs.target_event_json)_name != 'pull_request'
        shell: bash
        run: "set -euo pipefail\nNOW_EPOCH=$(date -u +%s)\nVERSION_CODE=$((NOW_EPOCH - 1500000000))\nif [ \"$VERSION_CODE\" -le 0 ] || [ \"$VERSION_CODE\" -gt 2100000000 ]; then\n  echo \"::error::Generated Android versionCode $VERSION_CODE is outside the Play-supported range\"\n  exit 1\nfi\necho \"HAIVE_VERSION_CODE=$VERSION_CODE\" >> \"$GITHUB_ENV\"\necho \"Android release versionCode: $VERSION_CODE\"\n"
      - name: Test shared domain
        run: ./gradlew :shared:desktopTest
      - name: Test Jules provider
        run: ./gradlew :providers:jules:desktopTest
      - name: Compile Jules provider Android
        run: ./gradlew :providers:jules:compileAndroidMain
      - name: Compile Jules provider JS
        run: ./gradlew :providers:jules:compileKotlinJs
      - name: Compile Jules provider Wasm
        run: ./gradlew :providers:jules:compileKotlinWasmJs
      - name: Test Android provider bootstrap
        run: ./gradlew :androidApp:testDebugUnitTest
      - name: Build Android (debug on PRs, release on push)
        env:
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: "if [ \"${{ fromJSON(inputs.target_event_json)_name }}\" = \"pull_request\" ]; then\n  ./gradlew :androidApp:assembleDebug\nelse\n  ./gradlew -Papp.versionCode=\"$HAIVE_VERSION_CODE\" :androidApp:assembleRelease :androidApp:bundleRelease\nfi\n"
      - name: Publish pinned Conveyance for isolated web builds
        run: "./gradlew -p vendor/conveyance-h2g2/vendor/Conveyance \\\n  :conveyance-core:publishToMavenLocal \\\n  :conveyance-compose:publishToMavenLocal\n"
      - name: Publish pinned h2g2 for isolated web builds
        run: ./gradlew -p vendor/conveyance-h2g2 publishToMavenLocal
      - name: Build Web JS
        run: ./gradlew -Phaive.useLocalH2g2=false -Phaive.useMavenLocalH2g2=true :webApp:jsBrowserProductionWebpack
      - name: Build Web Wasm
        run: ./gradlew -Phaive.useLocalH2g2=false -Phaive.useMavenLocalH2g2=true :webApp:wasmJsBrowserProductionWebpack
      - name: Stage GitHub Pages
        shell: bash
        run: "set -euo pipefail\nrm -rf webApp/build/pages\n\nJS_BUNDLE=\"$(find webApp/build -type f -path '*/js/productionExecutable/haive.js' -print -quit)\"\nWASM_BUNDLE=\"$(find webApp/build -type f -path '*/wasmJs/productionExecutable/haive.js' -print -quit)\"\nif [ -z \"$JS_BUNDLE\" ] || [ -z \"$WASM_BUNDLE\" ]; then\n  echo \"Production bundles found under webApp/build:\"\n  find webApp/build -type f -name 'haive.js' -print\n  exit 1\nfi\n\nJS_DIR=\"$(dirname \"$JS_BUNDLE\")\"\nWASM_DIR=\"$(dirname \"$WASM_BUNDLE\")\"\necho \"Staging JS bundle from $JS_DIR\"\necho \"Staging Wasm bundle from $WASM_DIR\"\n\nmkdir -p webApp/build/pages/wasm\ncp -R \"$JS_DIR\"/. webApp/build/pages/\ncp -R \"$WASM_DIR\"/. webApp/build/pages/wasm/\ncp -R webApp/src/webMain/resources/. webApp/build/pages/\ncp -R webApp/src/webMain/resources/. webApp/build/pages/wasm/\ntest -f webApp/build/pages/index.html\ntest -f webApp/build/pages/haive.js\ntest -f webApp/build/pages/wasm/index.html\ntest -f webApp/build/pages/wasm/haive.js\n"
      - name: Generate release notes
        if: fromJSON(inputs.target_event_json)_name != 'pull_request'
        shell: bash
        run: "set -euo pipefail\nmkdir -p fastlane/metadata/android/en-US/changelogs\nLAST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo \"\")\nif [ -n \"$LAST_TAG\" ]; then\n  git log \"${LAST_TAG}..HEAD\" --pretty=format:\"- %s\" --no-merges -n 50 \\\n    > fastlane/metadata/android/en-US/changelogs/default.txt\nelse\n  git log --pretty=format:\"- %s\" --no-merges -20 \\\n    > fastlane/metadata/android/en-US/changelogs/default.txt\nfi\necho \"=== Release notes ===\"\ncat fastlane/metadata/android/en-US/changelogs/default.txt\n"
      - name: Upload Android release inputs
        if: fromJSON(inputs.target_event_json)_name != 'pull_request'
        uses: actions/upload-artifact@v7
        with:
          name: haive-android-release
          path: "androidApp/build/outputs/apk/release/*.apk\nandroidApp/build/outputs/bundle/release/*.aab\n"
          if-no-files-found: error
      - name: Upload release notes
        if: fromJSON(inputs.target_event_json)_name != 'pull_request'
        uses: actions/upload-artifact@v7
        with:
          name: haive-release-notes
          path: fastlane/metadata/android/en-US/changelogs/
          if-no-files-found: error
      - name: Configure GitHub Pages
        if: fromJSON(inputs.target_event_json)_name != 'pull_request'
        uses: actions/configure-pages@v5
      - name: Upload GitHub Pages artifact
        if: fromJSON(inputs.target_event_json)_name != 'pull_request'
        uses: actions/upload-pages-artifact@v4
        with:
          path: webApp/build/pages
    needs: __central_check_start
    if: ${{ inputs.target_event_name != 'pull_request' || fromJSON(inputs.target_event_json).pull_request.head.repo.fork != true }}
  desktop-packages:
    name: Desktop installer (${{ matrix.platform }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux
            runner: ubuntu-latest
            task: packageDeb
            artifact: haive-desktop-linux
            path: desktopApp/build/compose/binaries/main/deb/*.deb
          - platform: windows
            runner: windows-latest
            task: packageMsi
            artifact: haive-desktop-windows
            path: desktopApp/build/compose/binaries/main/msi/*.msi
          - platform: macos
            runner: macos-latest
            task: packageDmg
            artifact: haive-desktop-macos
            path: desktopApp/build/compose/binaries/main/dmg/*.dmg
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
        with:
          repository: ${{ inputs.target_repository }}
          ref: ${{ inputs.target_sha }}
          token: ${{ github.token }}
          persist-credentials: false
      - name: Check out pinned h2g2
        uses: actions/checkout@v7
        with:
          repository: HereLiesAz/conveyance-h2g2
          ref: 8da4f0b3a764fe6ecd96f9e32c6ba7d3b2eb900a
          path: vendor/conveyance-h2g2
          token: ${{ github.token }}
          persist-credentials: false
      - name: Check out pinned Conveyance
        uses: actions/checkout@v7
        with:
          repository: HereLiesAz/Conveyance
          ref: b3e13674df9dfbcc0b35f800b57d78a305d07b03
          path: vendor/conveyance-h2g2/vendor/Conveyance
          token: ${{ github.token }}
          persist-credentials: false
      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'
      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v6
      - name: Verify desktop icons exist
        shell: bash
        run: "test -f branding/haive-icon-color.png || { echo \"Missing haive-icon-color.png\"; exit 1; }\ntest -f branding/haive-icon.ico || { echo \"Missing haive-icon.ico\"; exit 1; }\ntest -f branding/haive-icon.icns || { echo \"Missing haive-icon.icns\"; exit 1; }\n"
      - name: Publish pinned Conveyance to Maven Local
        shell: bash
        run: "./gradlew -p vendor/conveyance-h2g2/vendor/Conveyance \\\n  :conveyance-core:publishToMavenLocal \\\n  :conveyance-compose:publishToMavenLocal\n"
      - name: Publish pinned h2g2 to Maven Local
        shell: bash
        run: ./gradlew -p vendor/conveyance-h2g2 publishToMavenLocal
      - name: Package native installer
        shell: bash
        run: "set +e\n./gradlew -Phaive.useLocalH2g2=false -Phaive.useMavenLocalH2g2=true \":desktopApp:${{ matrix.task }}\"\nEXIT_CODE=$?\nif [ \"$EXIT_CODE\" -ne 0 ]; then\n  echo \"=== Native packaging diagnostics ===\"\n  if [ -d desktopApp/build/compose/logs ]; then\n    find desktopApp/build/compose/logs -type f -print | while IFS= read -r log_file; do\n      echo \"--- $log_file ---\"\n      cat \"$log_file\"\n    done\n  else\n    echo \"No Compose packaging logs directory found.\"\n  fi\nfi\nexit \"$EXIT_CODE\"\n"
      - name: Upload native installer
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.path }}
          if-no-files-found: error
    needs: __central_check_start
  release:
    name: Publish rolling alpha release
    if: ${{ (fromJSON(inputs.target_event_json)_name != 'pull_request' && inputs.target_ref == 'refs/heads/main') && (inputs.target_event_name != 'pull_request' || fromJSON(inputs.target_event_json).pull_request.head.repo.fork != true) }}
    needs:
      - __central_check_start
      - build
      - desktop-packages
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          repository: ${{ inputs.target_repository }}
          ref: ${{ inputs.target_sha }}
          token: ${{ github.token }}
          persist-credentials: false
      - name: Download Android release inputs
        uses: actions/download-artifact@v8
        with:
          name: haive-android-release
          path: release-input/android
      - name: Download Linux installer
        uses: actions/download-artifact@v8
        with:
          name: haive-desktop-linux
          path: release-input/linux
      - name: Download Windows installer
        uses: actions/download-artifact@v8
        with:
          name: haive-desktop-windows
          path: release-input/windows
      - name: Download macOS installer
        uses: actions/download-artifact@v8
        with:
          name: haive-desktop-macos
          path: release-input/macos
      - name: Download release notes
        uses: actions/download-artifact@v8
        with:
          name: haive-release-notes
          path: release-input/notes
      - name: Prepare release assets
        shell: bash
        run: "set -euo pipefail\n\nVERSION_NAME=$(grep '^app.versionName=' gradle.properties | cut -d'=' -f2- | tr -d '\\r ')\nif [ -z \"$VERSION_NAME\" ]; then\n  echo \"::error::app.versionName is missing from gradle.properties\"\n  exit 1\nfi\n\nMAJOR_MINOR=$(printf '%s' \"$VERSION_NAME\" | sed -E 's/^([0-9]+\\.[0-9]+).*/\\1/')\nTAG_NAME=\"latest-alpha-v${MAJOR_MINOR}\"\n\nmkdir -p release-files\n\nAPK_SOURCE=$(find release-input/android -type f -name '*.apk' -print -quit)\nDEB_SOURCE=$(find release-input/linux -type f -name '*.deb' -print -quit)\nMSI_SOURCE=$(find release-input/windows -type f -name '*.msi' -print -quit)\nDMG_SOURCE=$(find release-input/macos -type f -name '*.dmg' -print -quit)\n\nfor entry in \\\n  \"APK_SOURCE:$APK_SOURCE\" \\\n  \"DEB_SOURCE:$DEB_SOURCE\" \\\n  \"MSI_SOURCE:$MSI_SOURCE\" \\\n  \"DMG_SOURCE:$DMG_SOURCE\"; do\n  label=${entry%%:*}\n  value=${entry#*:}\n  if [ -z \"$value\" ]; then\n    echo \"::error::Missing release asset: $label\"\n    find release-input -type f -print\n    exit 1\n  fi\ndone\n\ncp \"$APK_SOURCE\" \"release-files/TheHaive-${VERSION_NAME}-android.apk\"\ncp \"$DEB_SOURCE\" \"release-files/$(basename \"$DEB_SOURCE\")\"\ncp \"$MSI_SOURCE\" \"release-files/$(basename \"$MSI_SOURCE\")\"\ncp \"$DMG_SOURCE\" \"release-files/$(basename \"$DMG_SOURCE\")\"\n\nNOTES_SOURCE=$(find release-input/notes -type f -name 'default.txt' -print -quit)\n{\n  echo \"# The Haive ${VERSION_NAME}\"\n  echo\n  echo \"Rolling alpha build from commit \\`${TARGET_SHA}\\`.\"\n  if [ -n \"$NOTES_SOURCE\" ] && [ -s \"$NOTES_SOURCE\" ]; then\n    echo\n    echo \"## Changes\"\n    cat \"$NOTES_SOURCE\"\n  fi\n} > release-notes.md\n\necho \"VERSION_NAME=$VERSION_NAME\" >> \"$GITHUB_ENV\"\necho \"MAJOR_MINOR=$MAJOR_MINOR\" >> \"$GITHUB_ENV\"\necho \"TAG_NAME=$TAG_NAME\" >> \"$GITHUB_ENV\"\n\necho \"Release assets:\"\nls -lh release-files\n"
      - name: Publish rolling alpha release
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        shell: bash
        run: "set -euo pipefail\n\nLEGACY_TAG=\"latest-debug-v${MAJOR_MINOR}\"\nif gh release view \"$LEGACY_TAG\" >/dev/null 2>&1; then\n  gh release delete \"$LEGACY_TAG\" --yes --cleanup-tag\nelif git ls-remote --exit-code --tags origin \"refs/tags/$LEGACY_TAG\" >/dev/null 2>&1; then\n  git push origin \":refs/tags/$LEGACY_TAG\"\nfi\n\ngit config user.name \"github-actions[bot]\"\ngit config user.email \"41898282+github-actions[bot]@users.noreply.github.com\"\ngit tag -fa \"$TAG_NAME\" -m \"Latest Haive alpha ${VERSION_NAME}\"\ngit push origin \"$TAG_NAME\" --force\n\nif gh release view \"$TAG_NAME\" >/dev/null 2>&1; then\n  gh release edit \"$TAG_NAME\" \\\n    --title \"The Haive ${VERSION_NAME} — Latest Alpha\" \\\n    --notes-file release-notes.md \\\n    --prerelease\n  gh release upload \"$TAG_NAME\" release-files/* --clobber\nelse\n  gh release create \"$TAG_NAME\" release-files/* \\\n    --prerelease \\\n    --verify-tag \\\n    --title \"The Haive ${VERSION_NAME} — Latest Alpha\" \\\n    --notes-file release-notes.md\nfi\n"
  deploy-pages:
    if: fromJSON(inputs.target_event_json)_name != 'pull_request'
    needs:
      - __central_check_start
      - build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
  publish-play:
    if: ${{ ((fromJSON(inputs.target_event_json)_name == 'push' && inputs.target_ref == 'refs/heads/main') || (fromJSON(inputs.target_event_json)_name == 'workflow_dispatch' && fromJSON(inputs.target_inputs_json).publish_play)) && (inputs.target_event_name != 'pull_request' || fromJSON(inputs.target_event_json).pull_request.head.repo.fork != true) }}
    needs:
      - __central_check_start
      - build
    runs-on: ubuntu-latest
    steps:
      - name: Download Android release bundle
        uses: actions/download-artifact@v8
        with:
          name: haive-android-release
          path: android-release
      - name: Download release notes
        uses: actions/download-artifact@v8
        with:
          name: haive-release-notes
          path: fastlane/metadata/android/en-US/changelogs
      - name: Publish to Google Play internal track
        uses: r0adkll/upload-google-play@v1
        with:
          serviceAccountJsonPlainText: ${{ secrets.PLAY_SERVICE_ACCOUNT_JSON }}
          packageName: com.hereliesaz.haive
          releaseFiles: android-release/**/*.aab
          track: internal
          status: completed
          whatsNewDirectory: fastlane/metadata/android/en-US/changelogs
  __central_check_finish:
    if: ${{ always() }}
    needs:
      - __central_check_start
      - build
      - desktop-packages
      - release
      - deploy-pages
      - publish-play
    runs-on: ubuntu-latest
    steps:
      - name: Complete target check
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          CHECK_ID: ${{ needs.__central_check_start.outputs.check_id }}
          CHECK_NAME: Build and Deploy
          FAILED: ${{ contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled') }}
          DETAILS_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        shell: bash
        run: "set -euo pipefail\n[[ -n \"${CHECK_ID:-}\" ]] || exit 0\nif [[ \"$FAILED\" == \"true\" ]]; then conclusion=\"failure\"; summary=\"Central workflow failed. Open the linked run for details.\"; else conclusion=\"success\"; summary=\"Central workflow completed successfully.\"; fi\npayload=\"$(jq -n --arg conclusion \"$conclusion\" --arg title \"$CHECK_NAME\" --arg summary \"$summary\" --arg details \"$DETAILS_URL\" '{status:\"completed\",conclusion:$conclusion,details_url:$details,output:{title:$title,summary:$summary}}')\"\ngh api --method PATCH \"repos/${TARGET_REPOSITORY}/check-runs/${CHECK_ID}\" --input - <<<\"$payload\""
