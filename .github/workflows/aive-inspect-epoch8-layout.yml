# Generated for HereLiesAz/aive:.github/workflows/inspect-epoch8-layout.yml.
# Edit the registered source or synchronizer, not this compiled workflow.
# Generated shared catalog workflow. Do not edit by hand.
# catalog-source: shared/2418f19fc5ee071234fb95299deff9ae3fc217d3c2eb56b99d0bc403a0b52ee1
name: aive · Inspect epoch-8 memory artifacts
on:
  workflow_dispatch:
    inputs:
      target_repository:
        required: true
        type: string
      target_repository_id:
        required: true
        type: string
      target_repository_owner:
        required: true
        type: string
      target_repository_owner_id:
        required: true
        type: string
      target_sha:
        required: true
        type: string
      target_check_sha:
        required: true
        type: string
      target_ref:
        required: true
        type: string
      target_ref_name:
        required: false
        type: string
      target_ref_type:
        required: false
        type: string
      target_head_ref:
        required: false
        type: string
      target_base_ref:
        required: false
        type: string
      target_actor:
        required: true
        type: string
      target_actor_id:
        required: false
        type: string
      target_event_name:
        required: true
        type: string
      target_event_json:
        required: true
        type: string
      target_inputs_json:
        required: true
        type: string
      target_vars_json:
        required: true
        type: string
      target_run_id:
        required: true
        type: string
      target_run_number:
        required: false
        type: string
      target_run_attempt:
        required: false
        type: string
      target_workflow_ref:
        required: false
        type: string
      target_workflow_name:
        required: true
        type: string
      source_workflow_path:
        required: true
        type: string
      source_sha256:
        required: true
        type: string
permissions:
  contents: read
env:
  TARGET_REPOSITORY: ${{ inputs.target_repository }}
  TARGET_REPOSITORY_ID: ${{ inputs.target_repository_id }}
  TARGET_REPOSITORY_OWNER: ${{ inputs.target_repository_owner }}
  TARGET_REPOSITORY_OWNER_ID: ${{ inputs.target_repository_owner_id }}
  TARGET_SHA: ${{ inputs.target_sha }}
  TARGET_CHECK_SHA: ${{ inputs.target_check_sha }}
  TARGET_REF: ${{ inputs.target_ref }}
  TARGET_REF_NAME: ${{ inputs.target_ref_name }}
  TARGET_REF_TYPE: ${{ inputs.target_ref_type }}
  TARGET_HEAD_REF: ${{ inputs.target_head_ref }}
  TARGET_BASE_REF: ${{ inputs.target_base_ref }}
  TARGET_ACTOR: ${{ inputs.target_actor }}
  TARGET_ACTOR_ID: ${{ inputs.target_actor_id }}
  TARGET_EVENT_NAME: ${{ inputs.target_event_name }}
  TARGET_RUN_ID: ${{ inputs.target_run_id }}
  TARGET_RUN_NUMBER: ${{ inputs.target_run_number }}
  TARGET_RUN_ATTEMPT: ${{ inputs.target_run_attempt }}
  TARGET_WORKFLOW_REF: ${{ inputs.target_workflow_ref }}
  TARGET_WORKFLOW_NAME: ${{ inputs.target_workflow_name }}
jobs:
  central_check_start:
    runs-on: ubuntu-latest
    steps:
      - name: Validate immutable target identity
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        shell: bash
        run: "set -euo pipefail\nrepo_json=\"$(gh api \"repos/${TARGET_REPOSITORY}\")\"\nactual_repo_id=\"$(jq -r '.id' <<<\"$repo_json\")\"\nactual_owner_id=\"$(jq -r '.owner.id' <<<\"$repo_json\")\"\nactual_owner=\"$(jq -r '.owner.login' <<<\"$repo_json\")\"\n[[ \"$actual_repo_id\" == \"$TARGET_REPOSITORY_ID\" ]] || { echo \"Repository ID mismatch\" >&2; exit 1; }\n[[ \"$actual_owner_id\" == \"103241502\" ]] || { echo \"Repository is not owned by HereLiesAz\" >&2; exit 1; }\n[[ \"${actual_owner,,}\" == \"hereliesaz\" ]] || { echo \"Repository owner mismatch\" >&2; exit 1; }\n[[ \"$TARGET_REPOSITORY_OWNER_ID\" == \"103241502\" ]] || { echo \"OIDC owner ID mismatch\" >&2; exit 1; }"
      - name: Set target status pending
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          STATUS_CONTEXT: ${{ inputs.source_workflow_path }}
          DETAILS_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        shell: bash
        run: "set -euo pipefail\npayload=\"$(jq -n --arg state \"pending\" --arg context \"$STATUS_CONTEXT\" --arg description \"Running from HereLiesAz/workflows.\" --arg target_url \"$DETAILS_URL\" '{state:$state,context:$context,description:$description,target_url:$target_url}')\"\ngh api --method POST \"repos/${TARGET_REPOSITORY}/statuses/${TARGET_CHECK_SHA}\" --input - <<<\"$payload\""
  inspect:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
        with:
          repository: ${{ inputs.target_repository }}
          ref: ${{ inputs.target_sha }}
          token: ${{ secrets.GH_TOKEN }}
          persist-credentials: false
      - name: Download representative epoch-8 bundles
        shell: bash
        run: "set -euo pipefail\nmkdir -p /tmp/epoch8\ncurl -fL --retry 3 \\\n  -o /tmp/epoch8/association.tar.gz \\\n  https://github.com/HereLiesAz/aive/releases/download/memory-layer-epoch8/haive-specialist_08_association_linker-onnx-epoch8.tar.gz\ncurl -fL --retry 3 \\\n  -o /tmp/epoch8/noun.tar.gz \\\n  https://github.com/HereLiesAz/aive/releases/download/memory-layer-epoch8/haive-specialist_03_noun_indexer-int8-epoch8.tar.gz\nsha256sum /tmp/epoch8/*.tar.gz\n"
      - name: List and extract bundles
        shell: bash
        run: "set -euo pipefail\necho '=== ASSOCIATION ARCHIVE ==='\ntar -tzf /tmp/epoch8/association.tar.gz | sed -n '1,240p'\necho '=== NOUN ARCHIVE ==='\ntar -tzf /tmp/epoch8/noun.tar.gz | sed -n '1,240p'\nmkdir -p /tmp/epoch8/association /tmp/epoch8/noun\ntar -xzf /tmp/epoch8/association.tar.gz -C /tmp/epoch8/association\ntar -xzf /tmp/epoch8/noun.tar.gz -C /tmp/epoch8/noun\necho '=== EXTRACTED FILES ==='\nfind /tmp/epoch8/association /tmp/epoch8/noun -type f -printf '%p\\t%s bytes\\n' | sort\n"
      - name: Inspect manifests and tokenizer metadata
        shell: bash
        run: "set -euo pipefail\nwhile IFS= read -r file; do\n  echo \"=== $file ===\"\n  sed -n '1,220p' \"$file\"\ndone < <(find /tmp/epoch8/association /tmp/epoch8/noun -type f \\\n  \\( -name '*.json' -o -name '*.txt' -o -name '*.md' -o -name '*.jinja' \\) \\\n  -size -256k | sort)\n"
      - name: Inspect ONNX graph contracts
        shell: bash
        run: "set -euo pipefail\npython -m pip install --quiet 'onnx>=1.17,<2'\npython - <<'PY'\nfrom pathlib import Path\nimport onnx\n\nroots = [Path('/tmp/epoch8/association'), Path('/tmp/epoch8/noun')]\nfor root in roots:\n    for path in sorted(root.rglob('*.onnx')):\n        print(f'=== ONNX {path} ===')\n        model = onnx.load(str(path), load_external_data=False)\n        print('ir_version:', model.ir_version)\n        print('opsets:', [(o.domain, o.version) for o in model.opset_import])\n        print('metadata:', {p.key: p.value for p in model.metadata_props})\n        initializer_names = {x.name for x in model.graph.initializer}\n        print('inputs:')\n        for value in model.graph.input:\n            if value.name in initializer_names:\n                continue\n            t = value.type.tensor_type\n            shape = []\n            for d in t.shape.dim:\n                shape.append(d.dim_param or d.dim_value or '?')\n            print(' ', value.name, 'elem_type=', t.elem_type, 'shape=', shape)\n        print('outputs:')\n        for value in model.graph.output:\n            t = value.type.tensor_type\n            shape = []\n            for d in t.shape.dim:\n                shape.append(d.dim_param or d.dim_value or '?')\n            print(' ', value.name, 'elem_type=', t.elem_type, 'shape=', shape)\nPY\n"
      - name: Execute Specialist 08 and probe tokenizers
        shell: bash
        run: "set -euo pipefail\npython -m pip install --quiet 'onnxruntime>=1.23,<2' 'tokenizers>=0.22,<1' 'transformers>=4.57,<6' 'numpy>=2,<3'\npython - <<'PY'\nfrom pathlib import Path\nimport numpy as np\nimport onnxruntime as ort\nfrom tokenizers import Tokenizer\nfrom transformers import AutoTokenizer\n\nassoc = Path('/tmp/epoch8/association/onnx')\ntokenizer = Tokenizer.from_file(str(assoc / 'tokenizer.json'))\ntokenizer.enable_padding()\ntokenizer.enable_truncation(max_length=256)\ntexts = [\n    'API timeout is configured for 30 seconds.',\n    'API timeout is configured for 60 seconds.',\n    'The UI uses a dark theme.',\n]\nencoded = tokenizer.encode_batch(texts)\nmax_len = max(len(e.ids) for e in encoded)\nids = np.asarray([e.ids + [0] * (max_len - len(e.ids)) for e in encoded], dtype=np.int64)\nmasks = np.asarray([e.attention_mask + [0] * (max_len - len(e.attention_mask)) for e in encoded], dtype=np.int64)\ntypes = np.asarray([e.type_ids + [0] * (max_len - len(e.type_ids)) for e in encoded], dtype=np.int64)\n\nsess = ort.InferenceSession(str(assoc / 'model.onnx'), providers=['CPUExecutionProvider'])\noutput_names = [o.name for o in sess.get_outputs()]\nvalues = sess.run(None, {\n    'input_ids': ids,\n    'attention_mask': masks,\n    'token_type_ids': types,\n})\nprint('association output names:', output_names)\nfor name, value in zip(output_names, values):\n    print('association output:', name, value.shape, value.dtype)\nhidden = values[output_names.index('last_hidden_state')]\npooled_name = next(name for name in output_names if name != 'last_hidden_state')\npooled = values[output_names.index(pooled_name)]\nexpanded = masks[..., None].astype(np.float32)\nmanual = (hidden * expanded).sum(axis=1) / np.clip(expanded.sum(axis=1), 1e-9, None)\nmanual /= np.clip(np.linalg.norm(manual, axis=1, keepdims=True), 1e-12, None)\nprint('pooled output name:', pooled_name)\nprint('pooled norms:', np.linalg.norm(pooled, axis=1).tolist())\nprint('manual-normalized max abs diff:', float(np.max(np.abs(pooled - manual))))\nnormalized = pooled / np.clip(np.linalg.norm(pooled, axis=1, keepdims=True), 1e-12, None)\nprint('similarity timeout30-timeout60:', float(normalized[0] @ normalized[1]))\nprint('similarity timeout30-ui:', float(normalized[0] @ normalized[2]))\n\nnoun_root = Path('/tmp/epoch8/noun')\nraw_qwen = Tokenizer.from_file(str(noun_root / 'tokenizer.json'))\nprint('qwen tokenizer model:', type(raw_qwen.model).__name__)\nprint('qwen pre_tokenizer:', raw_qwen.pre_tokenizer)\nprint('qwen decoder:', raw_qwen.decoder)\nprint('qwen post_processor:', raw_qwen.post_processor)\n\nhf = AutoTokenizer.from_pretrained(str(noun_root), local_files_only=True, trust_remote_code=False)\nrendered = hf.apply_chat_template(\n    [{'role': 'user', 'content': 'Return one JSON object with noun/entity tags for this packet.'}],\n    tokenize=False,\n    add_generation_prompt=True,\n)\ntoken_ids = hf.apply_chat_template(\n    [{'role': 'user', 'content': 'Return one JSON object with noun/entity tags for this packet.'}],\n    tokenize=True,\n    add_generation_prompt=True,\n)\nprint('qwen chat template rendered repr:', repr(rendered))\nprint('qwen chat token ids:', token_ids[:80])\nprint('qwen chat token count:', len(token_ids))\nprint('qwen eos token/id:', hf.eos_token, hf.eos_token_id)\nprint('qwen pad token/id:', hf.pad_token, hf.pad_token_id)\nPY\n"
    needs: central_check_start
    if: ${{ inputs.target_event_name != 'pull_request' || fromJSON(inputs.target_event_json).pull_request.head.repo.fork != true }}
    permissions:
      contents: read
    environment: aive
  central_check_finish:
    if: ${{ always() }}
    needs:
      - central_check_start
      - inspect
    runs-on: ubuntu-latest
    steps:
      - name: Complete target status
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          STATUS_CONTEXT: ${{ inputs.source_workflow_path }}
          FAILED: ${{ contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled') }}
          DETAILS_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        shell: bash
        run: "set -euo pipefail\nif [[ \"$FAILED\" == \"true\" ]]; then\n  state=\"failure\"\n  description=\"Central workflow failed.\"\nelse\n  state=\"success\"\n  description=\"Central workflow completed successfully.\"\nfi\npayload=\"$(jq -n --arg state \"$state\" --arg context \"$STATUS_CONTEXT\" --arg description \"$description\" --arg target_url \"$DETAILS_URL\" '{state:$state,context:$context,description:$description,target_url:$target_url}')\"\ngh api --method POST \"repos/${TARGET_REPOSITORY}/statuses/${TARGET_CHECK_SHA}\" --input - <<<\"$payload\""
run-name: aive · Inspect epoch-8 memory artifacts
