# Generated for HereLiesAz/hereliesaz.github.io:.github/workflows/dedup_scan.yml.
# Edit the registered source or synchronizer, not this compiled workflow.
# Generated shared catalog workflow. Do not edit by hand.
# catalog-source: shared/ba95e38b43009fb06f5e6f4f99de7a7d8e904705ab6a02916778061a5a5ce753
name: hereliesaz.github.io · Dedup Scan
on:
  workflow_dispatch:
    inputs:
      target_repository:
        required: true
        type: string
      target_repository_id:
        required: true
        type: string
      target_repository_owner:
        required: true
        type: string
      target_repository_owner_id:
        required: true
        type: string
      target_sha:
        required: true
        type: string
      target_check_sha:
        required: true
        type: string
      target_ref:
        required: true
        type: string
      target_ref_name:
        required: false
        type: string
      target_ref_type:
        required: false
        type: string
      target_head_ref:
        required: false
        type: string
      target_base_ref:
        required: false
        type: string
      target_actor:
        required: true
        type: string
      target_actor_id:
        required: false
        type: string
      target_event_name:
        required: true
        type: string
      target_event_json:
        required: true
        type: string
      target_inputs_json:
        required: true
        type: string
      target_vars_json:
        required: true
        type: string
      target_run_id:
        required: true
        type: string
      target_run_number:
        required: false
        type: string
      target_run_attempt:
        required: false
        type: string
      target_workflow_ref:
        required: false
        type: string
      target_workflow_name:
        required: true
        type: string
      source_workflow_path:
        required: true
        type: string
      source_sha256:
        required: true
        type: string
permissions:
  contents: read
env:
  TARGET_REPOSITORY: ${{ inputs.target_repository }}
  TARGET_REPOSITORY_ID: ${{ inputs.target_repository_id }}
  TARGET_REPOSITORY_OWNER: ${{ inputs.target_repository_owner }}
  TARGET_REPOSITORY_OWNER_ID: ${{ inputs.target_repository_owner_id }}
  TARGET_SHA: ${{ inputs.target_sha }}
  TARGET_CHECK_SHA: ${{ inputs.target_check_sha }}
  TARGET_REF: ${{ inputs.target_ref }}
  TARGET_REF_NAME: ${{ inputs.target_ref_name }}
  TARGET_REF_TYPE: ${{ inputs.target_ref_type }}
  TARGET_HEAD_REF: ${{ inputs.target_head_ref }}
  TARGET_BASE_REF: ${{ inputs.target_base_ref }}
  TARGET_ACTOR: ${{ inputs.target_actor }}
  TARGET_ACTOR_ID: ${{ inputs.target_actor_id }}
  TARGET_EVENT_NAME: ${{ inputs.target_event_name }}
  TARGET_RUN_ID: ${{ inputs.target_run_id }}
  TARGET_RUN_NUMBER: ${{ inputs.target_run_number }}
  TARGET_RUN_ATTEMPT: ${{ inputs.target_run_attempt }}
  TARGET_WORKFLOW_REF: ${{ inputs.target_workflow_ref }}
  TARGET_WORKFLOW_NAME: ${{ inputs.target_workflow_name }}
concurrency:
  group: dedup-scan-${{ inputs.target_repository }}-${{ inputs.target_event_name }}-hereliesaz.github.io
  cancel-in-progress: true
jobs:
  central_check_start:
    runs-on: ubuntu-latest
    steps:
      - name: Validate immutable target identity
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        shell: bash
        run: "set -euo pipefail\nrepo_owner=\"${TARGET_REPOSITORY%%/*}\"\n[[ -n \"$TARGET_REPOSITORY_ID\" ]] || { echo \"Missing repository ID\" >&2; exit 1; }\n[[ \"${repo_owner,,}\" == \"hereliesaz\" ]] || { echo \"Repository owner mismatch\" >&2; exit 1; }\n[[ \"${TARGET_REPOSITORY_OWNER,,}\" == \"hereliesaz\" ]] || { echo \"OIDC owner login mismatch\" >&2; exit 1; }\n[[ \"$TARGET_REPOSITORY_OWNER_ID\" == \"103241502\" ]] || { echo \"OIDC owner ID mismatch\" >&2; exit 1; }"
      - name: Set target status pending
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          STATUS_CONTEXT: ${{ inputs.source_workflow_path }}
          DETAILS_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        shell: bash
        run: "set -euo pipefail\npayload=\"$(jq -n --arg state \"pending\" --arg context \"$STATUS_CONTEXT\" --arg description \"Running from HereLiesAz/workflows.\" --arg target_url \"$DETAILS_URL\" '{state:$state,context:$context,description:$description,target_url:$target_url}')\"\nif ! output=\"$(gh api --method POST \"repos/${TARGET_REPOSITORY}/statuses/${TARGET_CHECK_SHA}\" --input - <<<\"$payload\" 2>&1)\"; then\n  echo \"::warning::Could not post target status: $output\"\nfi"
  smoke:
    if: ${{ (inputs.target_event_name == 'pull_request') && (inputs.target_event_name != 'pull_request' || fromJSON(inputs.target_event_json).pull_request.head.repo.fork != true) }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          repository: ${{ inputs.target_repository }}
          ref: ${{ inputs.target_sha }}
          token: ${{ secrets.GH_TOKEN }}
          persist-credentials: false
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install image readers
        run: python -m pip install --disable-pip-version-check Pillow pillow-heif
      - name: Compile scanner
        run: python -m py_compile scripts/dedup_scan.py
      - name: Verify exact and compressed-copy detection
        shell: bash
        run: "set -euo pipefail\ntmp=\"$(mktemp -d)\"\nmkdir -p \"$tmp/assets\"\npython - \"$tmp/assets\" <<'PY'\nfrom pathlib import Path\nimport shutil\nimport sys\nfrom PIL import Image, ImageDraw\n\nroot = Path(sys.argv[1])\nimage = Image.new(\"RGB\", (640, 480), \"white\")\ndraw = ImageDraw.Draw(image)\ndraw.rectangle((40, 40, 600, 430), fill=(20, 40, 80))\ndraw.ellipse((160, 90, 500, 420), fill=(220, 80, 40))\ndraw.line((40, 430, 600, 40), fill=(250, 240, 80), width=18)\nimage.save(root / \"original.jpg\", quality=96)\nshutil.copyfile(root / \"original.jpg\", root / \"exact-copy.jpg\")\nimage.save(root / \"compressed.jpg\", quality=38, optimize=True)\nPY\nprintf '{}\\n' > \"$tmp/meta.json\"\npython scripts/dedup_scan.py \\\n  --assets \"$tmp/assets\" \\\n  --meta \"$tmp/meta.json\" \\\n  --output \"$tmp/report.json\"\npython - \"$tmp/report.json\" <<'PY'\nimport json\nimport sys\n\nreport = json.load(open(sys.argv[1], encoding=\"utf-8\"))\nassert report[\"exactCount\"] >= 1, report\nassert report[\"compressedCount\"] >= 1, report\nprint(json.dumps({\n    \"exact\": report[\"exactCount\"],\n    \"compressed\": report[\"compressedCount\"],\n    \"similar\": report[\"similarCount\"],\n}))\nPY\n"
    needs: central_check_start
    permissions:
      contents: read
    environment: hereliesaz.github.io
  scan:
    if: ${{ (inputs.target_event_name == 'workflow_dispatch') && (inputs.target_event_name != 'pull_request' || fromJSON(inputs.target_event_json).pull_request.head.repo.fork != true) }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          repository: ${{ inputs.target_repository }}
          ref: ${{ inputs.target_sha }}
          token: ${{ secrets.GH_TOKEN }}
          persist-credentials: false
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install image readers
        run: python -m pip install --disable-pip-version-check Pillow pillow-heif
      - name: Scan for duplicates and similar images
        run: "python scripts/dedup_scan.py \\\n  --assets public/assets \\\n  --meta public/meta.json \\\n  --output dedup-report.json\n"
      - name: Upload dedup report
        uses: actions/upload-artifact@v4
        with:
          name: dedup-report-${{ fromJSON(inputs.target_inputs_json).request_id }}
          path: dedup-report.json
          if-no-files-found: error
          retention-days: 7
    needs: central_check_start
    permissions:
      contents: read
    environment: hereliesaz.github.io
  central_check_finish:
    if: ${{ always() }}
    needs:
      - central_check_start
      - smoke
      - scan
    runs-on: ubuntu-latest
    steps:
      - name: Complete target status
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          STATUS_CONTEXT: ${{ inputs.source_workflow_path }}
          FAILED: ${{ contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled') }}
          DETAILS_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        shell: bash
        run: "set -euo pipefail\nif [[ \"$FAILED\" == \"true\" ]]; then\n  state=\"failure\"\n  description=\"Central workflow failed.\"\nelse\n  state=\"success\"\n  description=\"Central workflow completed successfully.\"\nfi\npayload=\"$(jq -n --arg state \"$state\" --arg context \"$STATUS_CONTEXT\" --arg description \"$description\" --arg target_url \"$DETAILS_URL\" '{state:$state,context:$context,description:$description,target_url:$target_url}')\"\ngh api --method POST \"repos/${TARGET_REPOSITORY}/statuses/${TARGET_CHECK_SHA}\" --input - <<<\"$payload\""
run-name: hereliesaz.github.io · Dedup Scan
