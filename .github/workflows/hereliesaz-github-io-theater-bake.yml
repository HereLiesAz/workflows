# Generated for HereLiesAz/hereliesaz.github.io:.github/workflows/theater_bake.yml.
# Edit the registered source or synchronizer, not this compiled workflow.
# Generated shared catalog workflow. Do not edit by hand.
# catalog-source: shared/c60c9a0a0eb9673a77e05c80165b8606ebd1f25cc9f05d0ec7150ab4ec2f2687
name: hereliesaz.github.io · Theater Bake
on:
  workflow_dispatch:
    inputs:
      target_repository:
        required: true
        type: string
      target_repository_id:
        required: true
        type: string
      target_repository_owner:
        required: true
        type: string
      target_repository_owner_id:
        required: true
        type: string
      target_sha:
        required: true
        type: string
      target_check_sha:
        required: true
        type: string
      target_ref:
        required: true
        type: string
      target_ref_name:
        required: false
        type: string
      target_ref_type:
        required: false
        type: string
      target_head_ref:
        required: false
        type: string
      target_base_ref:
        required: false
        type: string
      target_actor:
        required: true
        type: string
      target_actor_id:
        required: false
        type: string
      target_event_name:
        required: true
        type: string
      target_event_json:
        required: true
        type: string
      target_inputs_json:
        required: true
        type: string
      target_vars_json:
        required: true
        type: string
      target_run_id:
        required: true
        type: string
      target_run_number:
        required: false
        type: string
      target_run_attempt:
        required: false
        type: string
      target_workflow_ref:
        required: false
        type: string
      target_workflow_name:
        required: true
        type: string
      source_workflow_path:
        required: true
        type: string
      source_sha256:
        required: true
        type: string
permissions:
  contents: read
env:
  DEFAULT_IDS: 2020-12-02(10),2020-12-03(46),2023-10-15(24),2023-10-15(38),2023-10-15(5),20231129_043102~2,20240229_142440,IMG_20161216_123644,IMG_20180602_192220,IMG_20210611_073221_746,IMG_20220710_130450,IMG_20230127_191102_101,IMG_20240621_192348~2,IMG_20250606_194351094,PXL_20221026_233419488~4,PXL_20230323_234854707~2,PXL_20230527_000631951~2,PXL_20230613_074956361.NIGHT~2,PXL_20250124_105205423.RAW-01.COVER,PXL_20260113_131045314.RAW-01.COVER,2014-11-09 19.16.33,2020-12-03(4),2020-12-03,20220221_092654_HDR-01,20220803_152450~2(1),2023-10-15(28),IMG_0232,IMG_0607,IMG_20200609_225112_755,IMG_20230414_062745_265,IMG_20240620_222621,IMG_20250721_023419194~2,PXL_20221027_132046545,PXL_20230107_104032885-01(1),PXL_20230613_074825716.NIGHT,PXL_20230729_005524300-02,PXL_20250322_233808118.RAW-01.COVER,PXL_20250508_213441321.RAW-01.COVER~9,PXL_20251003_003642766,PXL_20260113_070911696.RAW-01.COVER
  TARGET_REPOSITORY: ${{ inputs.target_repository }}
  TARGET_REPOSITORY_ID: ${{ inputs.target_repository_id }}
  TARGET_REPOSITORY_OWNER: ${{ inputs.target_repository_owner }}
  TARGET_REPOSITORY_OWNER_ID: ${{ inputs.target_repository_owner_id }}
  TARGET_SHA: ${{ inputs.target_sha }}
  TARGET_CHECK_SHA: ${{ inputs.target_check_sha }}
  TARGET_REF: ${{ inputs.target_ref }}
  TARGET_REF_NAME: ${{ inputs.target_ref_name }}
  TARGET_REF_TYPE: ${{ inputs.target_ref_type }}
  TARGET_HEAD_REF: ${{ inputs.target_head_ref }}
  TARGET_BASE_REF: ${{ inputs.target_base_ref }}
  TARGET_ACTOR: ${{ inputs.target_actor }}
  TARGET_ACTOR_ID: ${{ inputs.target_actor_id }}
  TARGET_EVENT_NAME: ${{ inputs.target_event_name }}
  TARGET_RUN_ID: ${{ inputs.target_run_id }}
  TARGET_RUN_NUMBER: ${{ inputs.target_run_number }}
  TARGET_RUN_ATTEMPT: ${{ inputs.target_run_attempt }}
  TARGET_WORKFLOW_REF: ${{ inputs.target_workflow_ref }}
  TARGET_WORKFLOW_NAME: ${{ inputs.target_workflow_name }}
concurrency:
  group: theater-bake-hereliesaz.github.io
  cancel-in-progress: false
jobs:
  central_check_start:
    runs-on: ubuntu-latest
    steps:
      - name: Validate immutable target identity
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        shell: bash
        run: "set -euo pipefail\nrepo_owner=\"${TARGET_REPOSITORY%%/*}\"\n[[ -n \"$TARGET_REPOSITORY_ID\" ]] || { echo \"Missing repository ID\" >&2; exit 1; }\n[[ \"${repo_owner,,}\" == \"hereliesaz\" ]] || { echo \"Repository owner mismatch\" >&2; exit 1; }\n[[ \"${TARGET_REPOSITORY_OWNER,,}\" == \"hereliesaz\" ]] || { echo \"OIDC owner login mismatch\" >&2; exit 1; }\n[[ \"$TARGET_REPOSITORY_OWNER_ID\" == \"103241502\" ]] || { echo \"OIDC owner ID mismatch\" >&2; exit 1; }"
      - name: Set target status pending
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          STATUS_CONTEXT: ${{ inputs.source_workflow_path }}
          DETAILS_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        shell: bash
        run: "set -euo pipefail\npayload=\"$(jq -n --arg state \"pending\" --arg context \"$STATUS_CONTEXT\" --arg description \"Running from HereLiesAz/workflows.\" --arg target_url \"$DETAILS_URL\" '{state:$state,context:$context,description:$description,target_url:$target_url}')\"\nif ! output=\"$(gh api --method POST \"repos/${TARGET_REPOSITORY}/statuses/${TARGET_CHECK_SHA}\" --input - <<<\"$payload\" 2>&1)\"; then\n  echo \"::warning::Could not post target status: $output\"\nfi"
  bake:
    runs-on: ubuntu-latest
    timeout-minutes: 330
    concurrency:
      group: art-data-publish-hereliesaz.github.io
      cancel-in-progress: false
    steps:
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          persist-credentials: false
          repository: ${{ inputs.target_repository }}
          ref: ${{ inputs.target_sha }}
          token: ${{ secrets.GH_TOKEN }}
      - name: Seed existing bakes from art-data (cache the expensive stages)
        uses: actions/checkout@v4
        with:
          ref: art-data
          path: _artdata
          persist-credentials: false
          repository: ${{ inputs.target_repository }}
          token: ${{ secrets.GH_TOKEN }}
        continue-on-error: true
      - name: Stage cached bakes into public/data
        run: "mkdir -p public/data\nif [ -d _artdata ] && [ -n \"$(ls -A _artdata 2>/dev/null | grep -v '^\\.git$')\" ]; then\n  rsync -a --exclude='.git' _artdata/ public/data/\n  echo \"seeded $(find public/data/theater -type f 2>/dev/null | wc -l) cached files\"\nelse\n  echo \"no art-data seed; every painting bakes from scratch\"\nfi\n"
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          cache: pip
      - name: Install dependencies
        run: pip install -r scripts/requirements.txt
      - name: Bake theater layers
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          IDS_INPUT: ${{ fromJSON(inputs.target_event_json).inputs.ids }}
        run: "IDS=\"$IDS_INPUT\"\nif [ -z \"$IDS\" ]; then IDS=\"$DEFAULT_IDS\"; fi\necho \"Baking: $IDS\"\npython scripts/theater_baker.py \\\n  --input  public/assets/ \\\n  --output public/data/theater/ \\\n  --ids \"$IDS\"\n"
      - name: Build hinge graph
        run: python scripts/pareidolia_index.py --data public/data/theater/
      - name: Validate baked theater output
        run: python scripts/validate_output.py --dir public/data/theater
      - name: Deploy to art-data
        uses: peaceiris/actions-gh-pages@373f7f263a76c20808c831209c920827a82a2847
        with:
          github_token: ${{ secrets.GH_TOKEN }}
          publish_dir: ./public/data
          publish_branch: art-data
          keep_files: true
          commit_message: 'Theater bake: ${{ fromJSON(inputs.target_event_json).inputs.ids }}'
    needs: central_check_start
    if: ${{ inputs.target_event_name != 'pull_request' || fromJSON(inputs.target_event_json).pull_request.head.repo.fork != true }}
    permissions:
      contents: write
    environment: hereliesaz.github.io
  central_check_finish:
    if: ${{ always() }}
    needs:
      - central_check_start
      - bake
    runs-on: ubuntu-latest
    steps:
      - name: Complete target status
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          STATUS_CONTEXT: ${{ inputs.source_workflow_path }}
          FAILED: ${{ contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled') }}
          DETAILS_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        shell: bash
        run: "set -euo pipefail\nif [[ \"$FAILED\" == \"true\" ]]; then\n  state=\"failure\"\n  description=\"Central workflow failed.\"\nelse\n  state=\"success\"\n  description=\"Central workflow completed successfully.\"\nfi\npayload=\"$(jq -n --arg state \"$state\" --arg context \"$STATUS_CONTEXT\" --arg description \"$description\" --arg target_url \"$DETAILS_URL\" '{state:$state,context:$context,description:$description,target_url:$target_url}')\"\ngh api --method POST \"repos/${TARGET_REPOSITORY}/statuses/${TARGET_CHECK_SHA}\" --input - <<<\"$payload\""
run-name: hereliesaz.github.io · Theater Bake
