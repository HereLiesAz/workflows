name: Merged Build & Release

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

# ⚡ Concurrency: Cancels the entire job if a new push comes in.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Least-privilege default token for all jobs. The build-and-release job overrides this
# with the elevated scopes it needs (tags/releases/issues) in its own permissions block.
permissions:
  contents: write

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      # OpenCV (Java + native via Prefab) is a plain Maven Central dependency now — Gradle resolves
      # it during the build, so there is no library-fetch step here anymore.

      - name: Inject Google Services
        env:
          GOOGLE_SERVICES_API_KEY: ${{ secrets.GOOGLE_SERVICES_API_KEY }}
          PROJECT_ID: ${{ secrets.PROJECT_ID }}
          CLIENT_ID: ${{ secrets.CLIENT_ID }}
          ARCORE_API_KEY: ${{ secrets.ARCORE_API_KEY }}
        run: |
          if [ -f "app/google-services.json.template" ]; then
            sed -e 's/{{\([^}]*\)}}/${\1}/g' app/google-services.json.template | envsubst > app/google-services.json
          fi
          echo "ARCORE_API_KEY=$ARCORE_API_KEY" >> local.properties

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6
        with:
          cache-read-only: false
          dependency-graph: generate-submit-and-upload

      - name: Run unit tests
        run: |
          chmod +x gradlew
          # Both flavors: `testDebugUnitTest` is ambiguous now that app/build.gradle.kts
          # declares the playstore/github distribution dimension.
          ./gradlew testGithubDebugUnitTest testPlaystoreDebugUnitTest test --continue --build-cache

      - name: Upload test reports
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: unit-test-reports
          path: '**/build/reports/tests/'

  build-and-release:
    needs: unit-tests
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
      checks: read

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      # OpenCV (Java + native via Prefab) is a plain Maven Central dependency now — Gradle resolves
      # it during the build, so there is no library-fetch step here anymore.

      - name: Inject Google Services
        env:
          GOOGLE_SERVICES_API_KEY: ${{ secrets.GOOGLE_SERVICES_API_KEY }}
          PROJECT_ID: ${{ secrets.PROJECT_ID }}
          CLIENT_ID: ${{ secrets.CLIENT_ID }}
          ARCORE_API_KEY: ${{ secrets.ARCORE_API_KEY }}
        run: |
          if [ -f "app/google-services.json.template" ]; then
            echo "Template found. Injecting secrets..."
            sed -e 's/{{\([^}]*\)}}/${\1}/g' app/google-services.json.template | envsubst > app/google-services.json
          else
            echo "Warning: google-services.json.template not found. Skipping injection."
          fi

          # Inject ARCore API Key into local.properties for build.gradle.kts to pick up
          echo "ARCORE_API_KEY=$ARCORE_API_KEY" >> local.properties

      - name: Decode signing keystore
        env:
          KEYSTORE_RAW: ${{ secrets.KEYSTORE_RAW }}
        run: |
          # Empty secret (e.g. on a fork without access) → skip; the debug build then signs with the
          # default debug key rather than failing the whole CI run.
          if [ -z "$KEYSTORE_RAW" ]; then
            echo "::warning::KEYSTORE_RAW secret is empty — building with the default debug key."
            exit 0
          fi
          echo "$KEYSTORE_RAW" | base64 -d > app/keystore.jks
          if [ ! -s app/keystore.jks ]; then
            echo "::error::Decoded keystore is empty — KEYSTORE_RAW is not valid base64."
            exit 1
          fi
          echo "Keystore decoded to app/keystore.jks ($(stat -c%s app/keystore.jks) bytes)"

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        with:
          cache-read-only: false
          # The unit-tests job already generates, submits and uploads the dependency graph for
          # this commit; keep it disabled here so the build job doesn't submit a duplicate.
          # 'true' is not a valid value for setup-gradle@v6 (enum: disabled / generate /
          # generate-and-submit / generate-submit-and-upload / generate-and-upload /
          # download-and-submit), which failed the action before the build could start.
          dependency-graph: disabled

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build with Gradle
        id: gradle-build
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          # Consumed by the signingConfigs.release block in app/build.gradle.kts. The debug build type
          # uses this release config when the keystore is present (see app/build.gradle.kts), keeping
          # the auto-published artifact's signature stable; absent it, the default debug key is used.
          KEYSTORE_FILE: ${{ github.workspace }}/app/keystore.jks
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: |
          set +e

          # Only the github flavor gets built and published here — playstore has its own
          # dedicated release pipeline (release-aab.yml → Play tracks), and the unit-tests
          # job above already compiles/tests the playstore flavor for CI coverage, so
          # assembling a playstore APK in this job would just burn build time on an
          # artifact nothing downstream uses.
          ./gradlew assembleGithubDebug --build-cache > build.log 2>&1

          EXIT_CODE=$?
          echo "exit_code=$EXIT_CODE" >> $GITHUB_OUTPUT
          cat build.log
          exit $EXIT_CODE

      # ------------------------------------------------------------------
      # FAILURE HANDLING
      # ------------------------------------------------------------------
      - name: Report Failure to Jules
        if: failure() && steps.gradle-build.outcome == 'failure'
        uses: actions/github-script@v9
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');
            let logTail = 'Log file not found.';
            try { logTail = fs.readFileSync('build.log', 'utf8').slice(-4000); } catch (e) {}
            
            const assignee = 'gemini-code-assist';
            
            try {
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title: `Build Failure on ${context.eventName === 'pull_request' ? 'PR #' + context.issue.number : context.ref}`,
                body: `@${assignee} /jules debug this build error\n\n**Log:**\n\`\`\`\n${logTail}\n\`\`\``,
                labels: ['jules', 'bug'],
                assignees: [assignee]
              });
            } catch (error) {
              console.log("Failed to create issue with assignee. Retrying without assignee.");
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title: `Build Failure on ${context.eventName === 'pull_request' ? 'PR #' + context.issue.number : context.ref}`,
                body: `@${assignee} /jules debug this build error\n\n**Log:**\n\`\`\`\n${logTail}\n\`\`\``,
                labels: ['jules', 'bug']
              });
            }

      # ------------------------------------------------------------------
      # RELEASE STEPS (Only run on a direct push to main — never on PRs or
      # feature branches. Publishing the prerelease tag on every branch push
      # force-updates the shared latest-debug tag and triggers flaky asset
      # uploads; scoping to main keeps CI green on feature branches.)
      # ------------------------------------------------------------------
      - name: Prepare Release Variables
        if: success() && github.event_name == 'push' && github.ref == 'refs/heads/main'
        id: release_vars
        run: |
          # version.properties is the SINGLE SOURCE OF TRUTH for the version. The gradle build already
          # incremented versionBuild (the Android versionCode) and versionPatch and baked them into the
          # artifact; read the SAME values here. NEVER derive the version from the git commit count —
          # that scheme collides with versionBuild and causes Play "downgrade" versionCode rejections.
          MAJOR=$(grep '^versionMajor=' version.properties | cut -d'=' -f2 | tr -d '\r ')
          MINOR=$(grep '^versionMinor=' version.properties | cut -d'=' -f2 | tr -d '\r ')
          PATCH=$(grep '^versionPatch=' version.properties | cut -d'=' -f2 | tr -d '\r ')
          BUILD_NUMBER=$(grep '^versionBuild=' version.properties | cut -d'=' -f2 | tr -d '\r ')

          VERSION_NAME="$MAJOR.$MINOR.$PATCH.$BUILD_NUMBER"
          
          TAG_NAME="latest-debug-v${MAJOR}.${MINOR}"
          
          # Dynamic App Name from settings.gradle
          if [ -f "settings.gradle.kts" ]; then
             APP_NAME=$(grep 'rootProject.name' settings.gradle.kts | sed -E "s/.*=.*[\"'](.*)[\"']/\1/")
          elif [ -f "settings.gradle" ]; then
             APP_NAME=$(grep 'rootProject.name' settings.gradle | sed -E "s/.*=.*[\"'](.*)[\"']/\1/")
          else
             APP_NAME="App"
          fi
          
          # Only the github flavor is published here. playstore has its own dedicated
          # publish path (release-aab.yml → Play tracks) — putting its debug APK on a
          # GitHub release too was redundant: nobody sideloading from GitHub wants the
          # Shizuku-only build, and Play is the only real distribution channel for it.
          GITHUB_APK_ORIGINAL=$(find app/build/outputs/apk/github/debug -name "*.apk" | head -n 1)

          if [ -z "$GITHUB_APK_ORIGINAL" ]; then
             echo "Error: expected a github flavor APK, found none"
             exit 1
          fi

          GITHUB_TARGET_NAME="${APP_NAME}-${VERSION_NAME}-github-debug.apk"

          # Rename for release
          mv "$GITHUB_APK_ORIGINAL" "$GITHUB_TARGET_NAME"

          echo "TAG_NAME=$TAG_NAME" >> $GITHUB_ENV
          echo "GITHUB_APK_FILE=$GITHUB_TARGET_NAME" >> $GITHUB_ENV
          
          # Package Build Tools (Optional)
          mkdir -p build_tools_package/tools build_tools_package/native
          [ -d "app/src/main/assets/tools" ] && cp -r app/src/main/assets/tools/* build_tools_package/tools/ || true
          [ -d "app/src/main/jniLibs/arm64-v8a" ] && cp -r app/src/main/jniLibs/arm64-v8a/* build_tools_package/native/ || true
          cd build_tools_package && zip -r ../build-tools.zip . && cd ..

      - name: Publish Release
        if: success() && github.event_name == 'push' && github.ref == 'refs/heads/main'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          # Force update tag to point to current commit
          git tag -fa $TAG_NAME -m "Latest Debug Build"
          git push origin $TAG_NAME --force

          BUILD_TOOLS="build-tools.zip"

          # Check if release exists
          if gh release view $TAG_NAME > /dev/null 2>&1; then
             echo "Updating existing release..."

             # Every APK filename embeds the version (…-VERSION-github-debug.apk), and
             # versionBuild auto-increments every single run — so --clobber (which only
             # replaces an asset with an EXACT name match) never touched the previous
             # build's APK at all. Every run left its predecessor behind: this release
             # accumulated 18 versioned "-github-debug.apk" files before anyone noticed,
             # with no obvious "latest" among them and a very real chance of someone
             # grabbing a stale one — which Android then refuses to install over a newer
             # copy already on the device (INSTALL_FAILED_VERSION_DOWNGRADE). A release
             # titled "Latest Debug Build" should only ever have the latest build's APK
             # on it, so delete every prior "*-github-debug.apk" asset before uploading
             # the new one — matching the build-tools.zip cleanup already done here.
             for old in $(gh release view "$TAG_NAME" --json assets --jq '.assets[].name' | grep -- '-github-debug\.apk$' || true); do
               gh release delete-asset "$TAG_NAME" "$old" -y || true
             done
             gh release delete-asset "$TAG_NAME" "$BUILD_TOOLS" -y || true

             gh release edit $TAG_NAME --prerelease \
               --title "Latest Debug Build ($TAG_NAME)" \
               --notes "Auto-build from commit $GITHUB_SHA" \
               --target $GITHUB_SHA

             gh release upload $TAG_NAME "$GITHUB_APK_FILE"
             [ -f "$BUILD_TOOLS" ] && gh release upload $TAG_NAME "$BUILD_TOOLS"
          else
             echo "Creating new release..."
             gh release create $TAG_NAME "$GITHUB_APK_FILE" --prerelease \
               --title "Latest Debug Build ($TAG_NAME)" \
               --notes "Auto-build from commit $GITHUB_SHA" \
               --target $GITHUB_SHA
             [ -f "$BUILD_TOOLS" ] && gh release upload $TAG_NAME "$BUILD_TOOLS"
          fi
