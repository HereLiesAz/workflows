name: '😈 Glee Audit'

on:
  pull_request_target:
    types: [opened]

permissions:
  contents: read
  issues: write
  pull-requests: read

jobs:
  glee:
    uses: HereLiesAz/workflows-starter-template/.github/workflows/jules-glee-reusable.yml@72b4e445b76a540a31689cfa07eb811db0031da6
    secrets:
      JULES_API_KEY: ${{ secrets.JULES_API_KEY }}
